{
	"profiles": [
		{
			"name": "Build",
			"tasks": [
				{
					"target": "${WORK_DIR_PATH}/${MAP_BASE_NAME}-compile.map",
					"type": "export"
				},
				{
					"parameters": "${MAP_FULL_NAME}",
					"tool": "${bsp}",
					"type": "tool"
				},
				{
					"parameters": "${MAP_BASE_NAME}.bsp",
					"tool": "${vis}",
					"type": "tool"
				},
				{
					"enabled": false,
					"parameters": "${MAP_BASE_NAME}.bsp",
					"tool": "${rad}",
					"type": "tool"
				}
			],
			"workdir": "${MAP_DIR_PATH}"
		}
	],
	"version": 1
}
